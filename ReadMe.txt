WebApiCore Project demonstrate use of Enity Framework with .Net core and CRUDE operations.
Using core to make sure it handles cross domain request. for some reason app.usecors in startup.cs is not working but using EnableCores() on controller class is working fine.
